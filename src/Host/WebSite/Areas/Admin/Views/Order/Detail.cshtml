@model string
<div id="app">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="~/">Home</a></li>
            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Admin</a></li>
            <li class="breadcrumb-item"><a asp-action="Index">Order</a></li>
            <li class="breadcrumb-item active" aria-current="page">Details</li>
        </ol>
    </nav>

    <ul class="nav nav-tabs mb-3" id="myTab" role="tablist">
        <li class="nav-item">
            <a class="nav-link active" id="order-tab" data-toggle="tab" href="#order" role="tab" aria-controls="order" aria-selected="true">Order</a>
        </li>
        @*
            <li class="nav-item">
                <a class="nav-link" id="items-tab" data-toggle="tab" href="#items" role="tab" aria-controls="items" aria-selected="false">Items</a>
            </li>*@
    </ul>
    <div class="tab-content" id="myTabContent">
        <div class="tab-pane fade show active" id="order" role="tabpanel" aria-labelledby="order-tab">
            <div class="form-group">
                <div class="form-row">
                    <div class="col-md-12">
                        <div class="row">
                            <div class="col-md-12 mt-2 align-self-end">
                                <label for="OrderNo">OrderNo</label>
                                <input id="OrderNo" type="text" class="form-control" v-model="order.orderNo" placeholder="OrderNo" maxlength="20" required>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        @*<div class="tab-pane fade" id="items" role="tabpanel" aria-labelledby="items-tab">
                <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#itemModal">Add</button>
                <hr />
                <table id="itemsTable" class="table table-striped table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th class="all">SKU</th>
                            <th class="min-desktop">Alias</th>
                            <th class="all">Color</th>
                            <th class="all">Size</th>
                            <th class="min-desktop">Inv Qty</th>
                            <th class="min-desktop"></th>
                        </tr>
                    </thead>
                </table>
            </div>*@
    </div>

</div>


@section Scripts{
    <script>var orderNoParam='@Model';</script>
    <partial name="Script" model='"admin/order/save"' />
}